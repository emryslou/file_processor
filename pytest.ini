[pytest]
minversion = 8.0
addopts = -ra -q --cov=file_processor --cov-report=markdown:file_processor/meta/test-cov-report.md
testpaths =
    tests
; testenvs =
;     FTP_USER=benz
;     FTP_PASS=erni1
;     FTP_HOME = /home/ftp_test
;     FTP_PORT=31175
;     FTP_FIXTURE_SCOPE=function
    # only affects ftpserver_TLS
    ; FTP_PORT_TLS = 31176
    ; FTP_HOME_TLS = /home/ftp_test_TLS
    ; FTP_CERTFILE = ./tests/test_keycert.pem